---
title: 常用命令
subtitle:
date: 2026-07-09T20:21:30+08:00
slug: ef9efaa
draft: false
description:
keywords:
weight: 0
categories:
  - 命令
collections:
  - 常用
tags:
  - shell

---

## 常用命令

```shell
echo "net.ipv4.tcp_congestion_control=bbr" >> /etc/sysctl.conf && sysctl -p

echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf && sysctl -p

echo "net.ipv6.conf.all.forwarding=1" >> /etc/sysctl.conf && sysctl -p

echo "net.ipv4.conf.all.rp_filter = 0" >> /etc/sysctl.conf && sysctl -p
echo "net.ipv4.conf.default.rp_filter = 0" >> /etc/sysctl.conf && sysctl -p
```

**创建swap**

```shell
fallocate -l 2G /swap
dd if=/dev/zero of=/swap bs=1M count=2048

chmod 600 /swap
mkswap /swap
swapon /swap

free -h

nano /etc/fstab
/swap none swap sw 0 0
```

`ps -aux | { head -n1;grep sing; }` `ps aux | awk 'NR==1 || /python/'`

**添加sshkey** `echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIE2vdK+/mEci/jLNkXTNizgzZegmRSlQF9JT0poaEhXU admin@swysgh.top" >> /root/.ssh/authorized_keys`

## 常用脚本

`bash <(curl -sL https://run.NodeQuality.com)`

`bash <(curl -sL IP.Check.Place)`

`bash <(curl -L https://github.com/spiritLHLS/ecs/raw/main/ecs.sh)`

## dd脚本

**国外**  `curl -O https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh || wget -O reinstall.sh $_ && bash reinstall.sh debian 13 --ssh-key "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIE2vdK+/mEci/jLNkXTNizgzZegmRSlQF9JT0poaEhXU admin@swysgh.top" --ssh-port 59522`

**国内**  `curl -O https://cnb.cool/bin456789/reinstall/-/git/raw/main/reinstall.sh || wget -O reinstall.sh $_ && bash reinstall.sh debian 13 --ssh-key "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIE2vdK+/mEci/jLNkXTNizgzZegmRSlQF9JT0poaEhXU admin@swysgh.top" --ssh-port 59522`

## **安装软件**

**speedtest-cli**  `bash <(curl -s https://packagecloud.io/install/repositories/ookla/speedtest-cli/script.deb.sh) && apt install speedtest -y`

**docker**  `bash <(curl -fsSL https://get.docker.com)`

**sing-box**  `bash <(curl -fsSL https://sing-box.app/deb-install.sh)`

```shell
sudo mkdir -p /etc/apt/keyrings &&
   sudo curl -fsSL https://sing-box.app/gpg.key -o /etc/apt/keyrings/sagernet.asc &&
   sudo chmod a+r /etc/apt/keyrings/sagernet.asc &&
   echo '
Types: deb
URIs: https://deb.sagernet.org/
Suites: *
Components: *
Enabled: yes
Signed-By: /etc/apt/keyrings/sagernet.asc
' | sudo tee /etc/apt/sources.list.d/sagernet.sources &&
   sudo apt-get update &&
   sudo apt-get install sing-box
```

**nexttrace**  `curl -sL nxtrace.org/nt |bash`

## **docker相关**

**docker网桥**  `docker network create sthk_network --driver bridge --subnet 172.18.0.0/16 --gateway 172.18.0.1`
